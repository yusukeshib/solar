import Color from 'color'

export default (bv) => {
  let r=0.0, g=0.0, b=0.0 

       if(bv < -0.4) bv = -0.4;
       if(bv >  2.0) bv = 2.0;
       if((bv>=-0.40)&&(bv<0.00)) { let t=(bv+0.40)/(0.00+0.40); r=0.61+(0.11*t)+(0.1*t*t); }
  else if((bv>= 0.00)&&(bv<0.40)) { let t=(bv-0.00)/(0.40-0.00); r=0.83+(0.17*t)          ; }
  else if((bv>= 0.40)&&(bv<2.10)) { let t=(bv-0.40)/(2.10-0.40); r=1.00                   ; }
       if((bv>=-0.40)&&(bv<0.00)) { let t=(bv+0.40)/(0.00+0.40); g=0.70+(0.07*t)+(0.1*t*t); }
  else if((bv>= 0.00)&&(bv<0.40)) { let t=(bv-0.00)/(0.40-0.00); g=0.87+(0.11*t)          ; }
  else if((bv>= 0.40)&&(bv<1.60)) { let t=(bv-0.40)/(1.60-0.40); g=0.98-(0.16*t)          ; }
  else if((bv>= 1.60)&&(bv<2.00)) { let t=(bv-1.60)/(2.00-1.60); g=0.82         -(0.5*t*t); }
       if((bv>=-0.40)&&(bv<0.40)) { let t=(bv+0.40)/(0.40+0.40); b=1.00                   ; }
  else if((bv>= 0.40)&&(bv<1.50)) { let t=(bv-0.40)/(1.50-0.40); b=1.00-(0.47*t)+(0.1*t*t); }
  else if((bv>= 1.50)&&(bv<1.94)) { let t=(bv-1.50)/(1.94-1.50); b=0.63         -(0.6*t*t); }

  return Color({
    r: 256* r,
    g: 256* g,
    b: 256* b
  }).rgbNumber()
}

